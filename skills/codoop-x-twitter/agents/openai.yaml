interface:
  display_name: "Codoop X Publisher"
  short_description: "Publish approved X posts through the official API."
  default_prompt: "Publish this explicitly approved X post through the official API."
